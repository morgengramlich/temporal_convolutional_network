from setuptools import setup

setup(
    name='tcn',
    version='1.0.0',
    description='PyTorch implementation of a temporal convolution network',
    url='https://github.com/morgengramlich/temporal_convolutional_network',
    packages=['tcn'],
    install_requires=['torch'],
)
